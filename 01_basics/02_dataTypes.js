"use strict"; // treat all JS code in this file as newer version
//alert ("hlo") // we are using nodejs, not browser
console.log(typeof 3 
    +
     3); // not a good practice, since code readability should be our first priority
console.log( typeof "hlo")
let state=null; // null is used to represent empty(nothing) value i.e., null is a standalone value
let  userCountry;
let isLoggedIn=false
console.log(typeof state); //object 
console.log(typeof userCountry); //undefined
console.log(typeof isLoggedIn);
console.log(null); 
console.log(undefined); 